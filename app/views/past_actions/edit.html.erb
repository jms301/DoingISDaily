<h1>Editing past_action</h1>

<% form_for(@past_action) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @past_action %> |
<%= link_to 'Back', past_actions_path %>